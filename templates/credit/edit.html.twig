{% extends 'base.html.twig' %}


{% block body %}
    <h1  style="text-align: center;">Modifier un Credit</h1>

    {{ include('credit/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-secondary" style="width: 200px;" href="{{ path('app_credit_index') }}">retourner à la liste</a>
    <br>
    <br>

    {{ include('credit/_delete_form.html.twig') }}
{% endblock %}
