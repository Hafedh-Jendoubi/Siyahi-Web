{% extends 'back_base.html.twig' %}

{% block title %}Achat{% endblock %}

{% block body %}
    <h1 class="text-center mb-4">Détails de l'Achat</h1>

    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="card">
                    <div class="card-body">
                        <table class="table">
                            <tbody>
                                <tr>
                                    <th>Id</th>
                                    <td>{{ achat.id }}</td>
                                </tr>
                                <tr>
                                    <th>Image</th>
                                    <td><img src="{{ asset('uploads/' ~achat.Image) }}" style=" width:150px; height:150px; " alt=""/></td>
                                </tr>
                                <tr>
                                    <th>Type</th>
                                    <td>{{ achat.Type }}</td>
                                </tr>
                                <tr>
                                    <th>Description</th>
                                    <td>{{ achat.Description }}</td>
                                </tr>
                            </tbody>
                        </table>

                        <div class="text-center mt-3">
                            <a href="{{ path('app_achat_index') }}" class="btn btn-secondary">Back to list</a>
                            <a href="{{ path('app_achat_edit', {'id': achat.id}) }}" class="btn btn-primary">Edit</a>
                            {{ include('achat/_delete_form.html.twig') }}
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
